from session_6_CI.base_file import a, b, add, subtract, multiply, divide


# print(f"The values of a and b imported from base_file.py are: a = {a}, b = {b}", "__name__ =", __name__)

print(f"add({a}, {b}) =", add(a, b))
print(f"subtract({a}, {b}) =", subtract(a, b))
print(f"multiply({a}, {b}) =", multiply(a, b))
print(f"divide({a}, {b}) =", divide(a, b))
